export class ActualTimeModel {

    public currentTime: string;
    public currentTimeLong: number;

    constructor(currentTime: string, currentTimeLong: number) {
        this.currentTime = currentTime;
        this.currentTimeLong = currentTimeLong;
    }

}
